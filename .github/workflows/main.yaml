name: "Main branch testing"

on:
  push:
    branches:
      - main

jobs:
#  Analyze:
#    uses: iperon-net/messenger/.github/workflows/_analyze.yaml@main
#
#  Testing:
#    needs: [Analyze]
#    uses: iperon-net/messenger/.github/workflows/_testing.yaml@main

  bump_version:
    name: "Bump version"
    runs-on: macos-15
    timeout-minutes: 5
#    needs: [Testing]
    steps:
      - name: ls -ls
        run: echo ls -ls

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: install cider
        run: dart pub global activate cider

      - name: bump version
        run: cider bump breaking --bump-build
